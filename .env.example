# Copy to `.env.local` (never commit secrets).

# Phase 3 — Writing/Speaking AI (optional; local engine works without it)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Phase 5 — Cloud accounts & sync (optional until you create a Supabase project)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
